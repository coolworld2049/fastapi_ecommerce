version: '3.9'

services:

  store_service:
    build: .
    command: ./start.sh
    env_file:
      - .env
    environment:
      - DEBUG=False
      - MONGODB_HOST=host.docker.internal
      - AUTH_SERVICE_URL=http://host.docker.internal:8001
    ports:
      - ${PORT}:${PORT}

